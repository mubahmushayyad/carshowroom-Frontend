import {useMemo,useState} from 'react';import {Box,Button,Grid,MenuItem,Paper,Table,TableBody,TableCell,TableHead,TableRow,TextField,Typography,Stack,IconButton,Tooltip,Dialog,DialogTitle,DialogContent,DialogActions,Alert,Checkbox,FormControlLabel} from '@mui/material';import AddIcon from '@mui/icons-material/Add';import EditIcon from '@mui/icons-material/Edit';import DeleteIcon from '@mui/icons-material/Delete';import {useApp} from '../../context/AppContext';import {COLORS,FUEL,STATUS,TRANSMISSION} from '../../utils/constants';import {money} from '../../utils/formatters';import {margin,marginLabel} from '../../utils/calculations';import {validateCar} from '../../utils/validators';import ConfirmDialog from '../../components/common/ConfirmDialog';import PageHeader from '../../components/common/PageHeader';import StatusChip from '../../components/common/StatusChip';import SearchBar from '../../components/common/SearchBar';
const empty={make:'',model:'',year:new Date().getFullYear(),variant:'',purchaseRate:'',sellingPrice:'',stock:0,fuel:'Petrol',transmission:'Automatic',mileage:'',engine:'',description:'',status:'Available',supplierId:'',colors:['White'],images:['https://images.unsplash.com/photo-1549924231-f129b911e442?auto=format&fit=crop&w=1200&q=80']};
export default function Cars(){const {cars,suppliers,saveCar,deleteCar}=useApp();const [q,setQ]=useState(''),[filter,setFilter]=useState({fuel:'',status:'',supplier:'',year:'',color:''}),[sort,setSort]=useState(''),[form,setForm]=useState(empty),[open,setOpen]=useState(false),[errors,setErrors]=useState({}),[editing,setEditing]=useState(false),[confirm,setConfirm]=useState(null);const rows=useMemo(()=>{let x=cars.filter(c=>`${c.id} ${c.make} ${c.model} ${c.variant}`.toLowerCase().includes(q.toLowerCase()));if(filter.fuel)x=x.filter(c=>c.fuel===filter.fuel);if(filter.status)x=x.filter(c=>c.status===filter.status);if(filter.supplier)x=x.filter(c=>c.supplierId===filter.supplier);if(filter.year)x=x.filter(c=>String(c.year)===filter.year);if(filter.color)x=x.filter(c=>c.colors.includes(filter.color));if(sort)x=[...x].sort((a,b)=>Number(b[sort])-Number(a[sort]));return x},[cars,q,filter,sort]);const openNew=()=>{setForm(empty);setEditing(false);setErrors({});setOpen(true)};const openEdit=c=>{setForm({...c});setEditing(true);setErrors({});setOpen(true)};const submit=e=>{e.preventDefault();const er=validateCar(form);setErrors(er);if(Object.keys(er).length)return;saveCar({...form,purchaseRate:Number(form.purchaseRate),sellingPrice:Number(form.sellingPrice),stock:Number(form.stock)},'Current Staff');setOpen(false)};return <><PageHeader title="Cars & Inventory" subtitle="Complete CRUD, search, filtering, sorting and profit logic" action={<Button variant="contained" startIcon={<AddIcon/>} onClick={openNew}>Add Car</Button>}/><Stack spacing={2} mb={2}><SearchBar value={q} onChange={setQ} placeholder="Search by ID, make, model or variant"/><Grid container spacing={1}>{[['fuel',FUEL],['status',Object.values(STATUS)],['supplier',suppliers.map(s=>s.id)],['color',COLORS]].map(([k,opts])=><Grid item xs={6} md={2} key={k}><TextField select fullWidth size="small" label={k} value={filter[k]} onChange={e=>setFilter({...filter,[k]:e.target.value})}><MenuItem value="">All</MenuItem>{opts.map(o=><MenuItem key={o} value={o}>{k==='supplier'?suppliers.find(s=>s.id===o)?.company:o}</MenuItem>)}</TextField></Grid>)}<Grid item xs={6} md={2}><TextField fullWidth size="small" label="Year" value={filter.year} onChange={e=>setFilter({...filter,year:e.target.value})}/></Grid><Grid item xs={6} md={2}><TextField select fullWidth size="small" label="Sort" value={sort} onChange={e=>setSort(e.target.value)}><MenuItem value="">Default</MenuItem><MenuItem value="sellingPrice">Price</MenuItem><MenuItem value="year">Year</MenuItem><MenuItem value="stock">Stock</MenuItem><MenuItem value="purchaseRate">Purchase</MenuItem></TextField></Grid></Grid></Stack><Paper sx={{overflow:'auto'}}><Table size="small"><TableHead><TableRow>{['ID','Vehicle','Price','Profit','Stock','Supplier','Status','Actions'].map(h=><TableCell key={h}>{h}</TableCell>)}</TableRow></TableHead><TableBody>{rows.map(c=><TableRow key={c.id}><TableCell>{c.id}</TableCell><TableCell><b>{c.make} {c.model}</b><br/><small>{c.year} • {c.variant}</small></TableCell><TableCell>{money(c.sellingPrice)}</TableCell><TableCell>{money(c.sellingPrice-c.purchaseRate)}<br/><small>{marginLabel(margin(c.sellingPrice,c.purchaseRate))} {margin(c.sellingPrice,c.purchaseRate).toFixed(1)}%</small></TableCell><TableCell>{c.stock}</TableCell><TableCell>{suppliers.find(s=>s.id===c.supplierId)?.company||'—'}</TableCell><TableCell><StatusChip status={c.status}/></TableCell><TableCell><Tooltip title="Edit"><IconButton onClick={()=>openEdit(c)}><EditIcon/></IconButton></Tooltip><Tooltip title="Delete"><IconButton color="error" onClick={()=>setConfirm(c)}><DeleteIcon/></IconButton></Tooltip></TableCell></TableRow>)}</TableBody></Table></Paper><ConfirmDialog open={!!confirm} message={`Delete ${confirm?.make} ${confirm?.model}? This cannot be undone.`} onClose={()=>setConfirm(null)} onConfirm={()=>{deleteCar(confirm.id,'Current Staff');setConfirm(null)}}/><Dialog open={open} onClose={()=>setOpen(false)} maxWidth="md" fullWidth><DialogTitle>{editing?'Edit Car':'Add Car'}</DialogTitle><form onSubmit={submit}><DialogContent><Grid container spacing={2}>{[['make','Make'],['model','Model'],['variant','Variant'],['year','Year'],['purchaseRate','Purchase Rate (PKR)'],['sellingPrice','Selling Price (PKR)'],['stock','Stock'],['mileage','Mileage'],['engine','Engine'],['description','Description']].map(([k,l])=><Grid item xs={12} sm={k==='description'?12:6} key={k}><TextField fullWidth label={l} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})} error={!!errors[k]} helperText={errors[k]} multiline={k==='description'}/></Grid>)}<Grid item xs={12} sm={6}><TextField select fullWidth label="Fuel" value={form.fuel} onChange={e=>setForm({...form,fuel:e.target.value})}>{FUEL.map(x=><MenuItem key={x} value={x}>{x}</MenuItem>)}</TextField></Grid><Grid item xs={12} sm={6}><TextField select fullWidth label="Transmission" value={form.transmission} onChange={e=>setForm({...form,transmission:e.target.value})}>{TRANSMISSION.map(x=><MenuItem key={x} value={x}>{x}</MenuItem>)}</TextField></Grid><Grid item xs={12} sm={6}><TextField select fullWidth label="Status" value={form.status} onChange={e=>setForm({...form,status:e.target.value})}>{Object.values(STATUS).map(x=><MenuItem key={x} value={x}>{x}</MenuItem>)}</TextField></Grid><Grid item xs={12} sm={6}><TextField select fullWidth label="Supplier" value={form.supplierId} onChange={e=>setForm({...form,supplierId:e.target.value})}>{suppliers.map(s=><MenuItem key={s.id} value={s.id}>{s.company}</MenuItem>)}</TextField></Grid><Grid item xs={12}><Typography fontWeight={700}>Available Colors</Typography>{COLORS.map(c=><FormControlLabel key={c} control={<Checkbox checked={form.colors.includes(c)} onChange={e=>setForm({...form,colors:e.target.checked?[...new Set([...form.colors,c])]:form.colors.filter(x=>x!==c)})}/>} label={c}/>) }{errors.colors&&<Alert severity="error">{errors.colors}</Alert>}</Grid></Grid></DialogContent><DialogActions><Button onClick={()=>setOpen(false)}>Cancel</Button><Button type="submit" variant="contained">Save Car</Button></DialogActions></form></Dialog></>}

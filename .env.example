# Point this at your group's real backend when it's ready, e.g.:
# VITE_API_URL=https://your-backend.example.com/api
#
# Until then, the mock API in /server works as a drop-in replacement:
# npm run mock-api   (serves the same REST contract on port 4000)
VITE_API_URL=http://localhost:4000/api
